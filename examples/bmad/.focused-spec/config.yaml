version: 2
specifications:
  documents:
    - match: _bmad-output/specs/spec-{scope}/scenarios.md
runners:
  pytest-functional:
    module: ./.focused-spec/runners/pytest.ts
    timeoutMs: 120000
    options:
      command: uv
      commandArgs: [run, --with, pytest, python]
