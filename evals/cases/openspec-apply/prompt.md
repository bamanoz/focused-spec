Implement the existing OpenSpec change `add-blocked-account-focused-spec`. Use the available OpenSpec apply and focused-spec skills. The completed change must use version-2 document layouts with explicit `scope: current` for `openspec/specs` and `{scope}` capture for changes, and replace all planned evidence with project-local TypeScript runner evidence for both the existing Go unit test and pytest functional test. Do not inspect the focused-spec package source or installed `dist/` implementation. Complete every change task and finish only after strict focused-spec validation and `--scope add-blocked-account-focused-spec` execution pass.
