Implement the existing OpenSpec change `add-blocked-account-focused-spec`. Use the available OpenSpec apply and focused-spec skills. The completed change must replace all planned evidence with project-local TypeScript runner evidence for both the existing Go unit test and pytest functional test. Do not inspect the focused-spec package source or installed `dist/` implementation. Complete every change task and finish only after strict focused-spec validation and change-scoped execution pass.
