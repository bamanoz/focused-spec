This project has a completed OpenSpec specification and Go/Python tests from an earlier feature change, written without focused-spec. Adopt the installed focused-spec CLI and skill while implementing the existing `link-blocked-auth-evidence` change through its normal apply and sync workflow. This change does not alter product code or tests: enroll the existing "Blocked account with valid credentials" outcome without duplicating it, preserve the other three native scenarios verbatim, and do not claim they have executable evidence. The resulting main scenario must be the durable unmarked owner of its ID and exact executable evidence; any same-ID scenario in a working document must revise that owner without redirecting or altering its selectors. Verify that main evidence really reports PASS for both language tests, including after the working document leaves configured discovery. Run relevant OpenSpec and focused-spec checks, address any errors without hiding previously existing behavior, and report precisely what is and is not covered. Do not inspect focused-spec implementation source or installed JavaScript implementation; its CLI help, public runner declarations, and installed skill are available.
