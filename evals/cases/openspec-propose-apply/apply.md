Apply the OpenSpec change `add-blocked-account-focused-spec` completely. Use the available OpenSpec apply and focused-spec skills. Implement version-2 baseline and named-scope document layouts plus project-local TypeScript runners for both existing test frameworks without inspecting the focused-spec package source or installed `dist/` implementation. Replace planned evidence, complete the tasks, and finish only after strict validation and `--scope add-blocked-account-focused-spec` evidence execution pass.
