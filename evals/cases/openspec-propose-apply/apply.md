Apply the OpenSpec change `add-blocked-account-focused-spec` completely. Use the available OpenSpec apply and focused-spec skills. Implement version-2 document layouts with explicit `scope: current` for `openspec/specs` and `{scope}` capture for changes, plus project-local TypeScript runners for both existing test frameworks without inspecting focused-spec package source or installed `dist/` implementation. Replace planned evidence, complete the tasks, and finish only after strict validation and `--scope add-blocked-account-focused-spec` evidence execution pass.
