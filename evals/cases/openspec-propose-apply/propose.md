Create an OpenSpec proposal named `add-blocked-account-focused-spec` for representing the existing behavior “a blocked account with otherwise valid credentials cannot authenticate” as one executable focused scenario backed by both the existing Go unit test and pytest functional test. Use the available planning skills and respect their planning boundary. Do not implement code, runners, or configuration in this turn.
