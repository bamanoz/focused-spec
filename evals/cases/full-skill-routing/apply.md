Now implement the planned change `add-blocked-account-focused-spec`. Choose the appropriate workflow from the available skills. Use version-2 baseline and named-scope document layouts plus project-local TypeScript runner plugins for both existing test frameworks without inspecting focused-spec package source or installed `dist/`. Resolve all planned evidence, complete the change tasks, and verify strict validation plus `--scope add-blocked-account-focused-spec` execution.
